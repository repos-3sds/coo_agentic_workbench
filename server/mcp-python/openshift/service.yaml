apiVersion: v1
kind: Service
metadata:
  name: npa-mcp-server
  labels:
    app: npa-mcp-server
    part-of: npa-workbench
spec:
  selector:
    app: npa-mcp-server
  ports:
    - name: http
      port: 3002
      targetPort: 3002
      protocol: TCP
  type: ClusterIP
