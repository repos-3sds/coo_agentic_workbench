apiVersion: v1
kind: ConfigMap
metadata:
  name: npa-mcp-config
  labels:
    app: npa-mcp-server
    part-of: npa-workbench
data:
  REST_PORT: "3002"
  ENV: "production"
  # Public URL — Dify uses this to call tool endpoints and SSE
  PUBLIC_URL: "https://gfm-npa-agentic-service.apps.ocpsit.uat.dbs.com"
